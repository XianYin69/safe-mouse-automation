# cursorrules: safe-mouse-automation
默认后台虚拟输入（鼠标+键盘 PostMessage，不移动物理光标、不抢焦点）+ HUD 只读浮窗（置顶/穿透/不抢焦点）+ 批量执行加速。
前台 SendInput 回退必须先取得用户同意；严禁为测试操作或切换用户的前台窗口。
所有操作过 safety_gate；删除/格式化/注册表/关机/杀命令一律拒绝；截图前后留痕；缓存只写用户缓存目录。
